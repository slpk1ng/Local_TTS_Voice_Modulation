* v1.2.7：新增 `现实时间感知` 功能，开启后 LLM 模型能够得知目前的时间；新增 `多角色配置` 和 `选择当前激活的角色标识符` 功能，通过插件内UI实现角色切换 或 WebUI 配置不同角色的提示词、人设等，记忆文件也会按角色分开；新增 `对话上下文总结` ，当历史记录过长时，自动调用 LLM 对旧对话进行摘要，压缩 token，避免“降智”和遗忘重要信息。

新增 `允许 LLM 自动判断情绪强度` 、`情绪强度对语音变化幅度的影响` 、`情绪强度对语音音色丰富度的影响` 等功能，开启这些功能可以让 TTS 生成的语音更加像真人。

WebUI 体验提升：
新增 `记忆搜索` ，在记忆文件列表中添加搜索框，按内容或文件名快速查找；新增 `配置备份/恢复` ，WebUI 一键导出/导入 _conf_schema.json 和角色记忆，方便迁移；批量操作增强：支持按角色批量删除、导出聊天记录为文本或 JSON。

将原本的多语气配置功能合并在了 `多角色配置` 中。

修复了一些已知bug。


* v1.2.2：修复了发送图片后识图模型和语言模型上下文可能不同步的问题；修复 WebUI 中打开聊天记录没有正常滚动到消息底部的bug；修复发送图片后使用 Ollama 本地识图模型没有转 Base64 导致识图模型不工作的bug（如果遇到Failed to download file from https URL host='multimedia.nt.qq.com.cn' file='download' len=235. HTTP status code: 400报错，请耐心等待几分钟，查看识图模型是否正常返回输出）


* v1.2.1：新增 `识图模型名称` 、 `图片回复模式` 、`识图模型请求超时时间` 等功能。现在可以用符合人设的语句来回复图片啦！不用担心官方 LLM 模型接入后导致人设分裂的问题出现啦！

修复了 v1.2.0 版本 `仅响应私聊` 无效的bug，如果还出现此bug，请尝试在 Astrbot 中关闭 主动回复（配置文件 → 扩展功能 → 主动回复）；修复没有 @bot 也会异常发送消息的bug。

修改 WebUI 聊天标题，将原本只显示 JSON 文件名私聊改为显示 `角色名和用户名的聊天` ；群聊则显示 `角色名在某群的聊天` ；新增双击打开历史聊天记录、按 esc 可返回角色记忆文件管理。


* v1.2.0：新增对 NapCat 平台的支持。

修复 `分开发送` 功能和其分支在QQ平台无效的bug（由于QQ官方限制，被动回复仍然一次最多回复5条消息）。

修复了开启深度思考后对于 LLM 模型可能把思考过程混在 content 字段里（或直接返回思考文本但没有 JSON 或因最大 token 被限制），导致解析失败的bug，现在还会遇到 JSON 解析失败时，请尝试调大模型上下文长度。

新增 `仅响应私聊` 、 `根据语音时长动态等待` 功能。


* v1.1.9：神了，我的提示词怎么不见了。修复三个填写提示词的选项消失的bug。


* v1.1.8：WebUI 中新增群成员名字显示、不同群成员发送的消息用不同颜色显示和指定删除某条聊天记录的功能；移除了删除的倒计时 。新增 `隔离对话` 功能，启用后，群成员的上下文独立。生成的记忆 JSON 文件也会每个人独立。

将 `语音分开发送` 功能改为 `分开发送` ，该功能中细分为 `语音分开发送` 和 `文本分开发送` 。（由于 QQ 官方机器人限制，一次回复中超过5条时会导致报错）



* v1.1.7：简化了填写api\_v2.py的场景，现在只需选择该文件所在的路径即可。修复了一些文本错误，增加了更详细的文本提示。

增加 `语音分开发送` 功能，开启后语音不再自动合并为长语音，默认关闭。

修改了插件 UI 的顺序。新增支持中、日、英、韩的文本输出。

WebUI 中新增 `查看聊天记录` 、 `全选` 、 `取消全选` 和 `反选` 功能，将原 `按角色名（A~Z）` 的分类更改为 `按角色` ，自动按照A\ ~Z分类，且每个不同的角色之间增加了明显的间隙。

更新了教程文档。



* v1.1.6：修复了WebUI只能搜索到某一个角色的bug。



* v1.1.5：【重要更新】修复了不同群聊或私聊中所有对话参杂在一起导致 LLM 模型出现混乱的bug。修改了 v1.1.2 的命名格式。

为插件新增了删除角色记忆的 WebUI ，更方便管理记忆。增加 WebUI中 管理记忆文件的 `分类` 功能 。

修改了插件 UI 的顺序和显示逻辑，比如启用 `语气渐变` 时才会显示 `呼吸间隙` 和 `交叉渐变长度` ；反之则隐藏。



* v1.1.4：移除了`下载默认语音包`的功能，请需要的人群自行前往文档顶部查看丛雨语气包下载的链接，不再提供插件内置下载。优化部分性能。



* v1.1.3：由于 astrbot 官方安全审查 bot 死了，导致无法及时上传最新版本，v1.0 仅能使用ollama的 qwen3.5:4b 模型，功能有限，请还在使用 v1.0 的老版本的人群强烈建议更新此插件。



* v1.1.2：【重要更新】对原来的`人格提示词`选项进行了更细致的拆分，分为`人格提示词`、`JSON提示词`和`补充提示词`三个选项，更方便大众用户进行对于自定义角色的修改。

增加可自定义的`模型可读上下文条数`、`是否启用插件默认语气`、`可自定义角色名称`、`大模型请求超时时间`、`开启深度思考`和`删除指定记忆文件`等多种新功能。

修复了一些文本错误，增加了一些文本说明，删除了`情绪映射配置`中的默认文本。

修复了`情绪映射配置`中用户自定义的语气会和默认语气一起被语言模型选中应用的bug（启用插件默认语气时无效是正常的，这个选项默认是打开的）。

对原来的`Ollama 服务地址`选项进行了更细致的拆分，分为`大语言模型 (LLM) 名称`、`LLM后端类型`、`LLM服务地址`和`本地模型API Key`四个选项，适配了更多本地模型（理论上，缺少测试）。

对不同的角色增加独立对话数据存档，一般在C:\\Users\\.astrbot\\data\\memories中，命名一般是你的角色名+群聊/私聊+ID+DATA.json ，存档可在插件中选择删除，也可直接到该路径删除文件。wav文件则是TTS语音生成时产生的缓存，也可在此处删除。



* v1.1.1：此版本仅修改了下载默认语音包的逻辑，可选更新。（后续版本中已移除该功能）



* v1.1.0：正式版本。

修复了同一段句子在应包含多种语气时，只有第一个语气有效的 bug。

增加`选择使用CPU或GPU合成`的选项，增加更方便的`一键启动TTS服务`，无需再额外打开命令行。

新增`自定义语言模型选择`、`自定义上下文长度`功能。

增加了对其他语言的支持（原只支持日语，其他语言未测试等待反馈）。

增加`TTS语气渐变`的功能（可能会增加延迟，决于你的显卡速度。默认开启，可自行选择开启或关闭），让语音能渐变切换语气，而不是生硬地直接变调。



* v1.0.0：首个正式版本。

新增外部目录自动扫描机制、详细人格设定、TTS 参数优化。

整合 GPT-SoVITS 全参数可视化配置，大幅优化回复速度。



#### 另：当 astrbot 官方安全审查 bot 又死了导致无法通过插件审核进入市场时（也可能是我24小时内提交超过了3次），请前往我的仓库查看最新版本。



#### 其他：

* 余波\~仓库链接：[https://github.com/slpk1ng/Aftermath](https://github.com/slpk1ng/Aftermath)。
* 丛雨AIpet仓库链接：[https://github.com/kuxiaowo/AIpet-Murasame](https://github.com/kuxiaowo/AIpet-Murasame)。
