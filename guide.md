\# 插件配置说明



\## 🧠 大语言模型（LLM）相关配置



\### 1. llm\_model\_name（大语言模型名称）

\*\*是什么\*\*：填你本地用的聊天模型的名字，比如 `qwen3.5:4b`、`qwen3:8b` 等。



\*\*有什么用\*\*：插件要用它来理解你的话，生成台词和判断情绪。



\*\*出错检查\*\*：如果模型名字填错了，插件会报错。先在命令行输入 `ollama list` 看有哪些模型，然后复制准确名字。如果模型没下载，要先 `ollama pull`。



\### 2. llm\_backend（LLM后端类型）

\*\*是什么\*\*：选你用哪种方式连接本地模型。`ollama` 是官方的，`openai` 兼容接口可以用 LM Studio、vLLM 等。



\*\*有什么用\*\*：决定插件怎么跟你的模型说话。



\*\*出错检查\*\*：如果选了 `openai` 但实际用的是 Ollama，可能连不上。检查你的模型服务到底开了哪个端口，选对就行。



\### 3. llm\_api\_key（本地模型API Key）

\*\*是什么\*\*：有些本地服务（如 LM Studio）需要填一个随便的字符串作为钥匙，Ollama 则留空。



\*\*有什么用\*\*：用来通过认证。



\*\*出错检查\*\*：如果用 Ollama 留空没问题；如果用 LM Studio，随便填 `lm-studio` 就行。如果填了还报错，检查服务地址和端口是否对。



\### 4. llm\_base\_url（LLM服务地址）

\*\*是什么\*\*：模型服务所在的网址，比如 `http://127.0.0.1:11434`。



\*\*有什么用\*\*：告诉插件去哪里找模型。



\*\*出错检查\*\*：如果模型服务没启动，或者地址写错，就会连不上。确认服务已启动，地址和端口正确。



\### 5. num\_ctx（上下文长度）

\*\*是什么\*\*：模型能记住多少字（token数）。



\*\*有什么用\*\*：越大，模型能记住更多历史对话，但更占显存、更慢。



\*\*出错检查\*\*：如果显存不够导致崩溃，调小这个值，比如 `4096`。



\### 6. history\_length（模型可读上下文条数）

\*\*是什么\*\*：插件最多把最近几条对话发给模型。



\*\*有什么用\*\*：控制模型能看到多少历史。太少可能记不住，太多可能变笨。



\*\*出错检查\*\*：如果模型总是复读或者变傻，把它调小，比如 `5` 或 `8`。



\### 7. enable\_think（开启深度思考）

\*\*是什么\*\*：让模型在回答前先想一会儿（比如 DeepSeek 的思考模式）。



\*\*有什么用\*\*：回答更聪明，但更慢。



\*\*出错检查\*\*：如果开了很慢，说明你的显卡不够快，可以关掉；如果没效果，检查你的模型是否支持思考模式。



\### 8. llm\_timeout（大模型请求超时时间）

\*\*是什么\*\*：插件等模型回复的最长时间（秒）。



\*\*有什么用\*\*：防止模型卡死导致插件一直等着。



\*\*出错检查\*\*：如果模型很慢，经常超时，调大这个值（比如 `180`）。



\---



\## 🔊 TTS 语音相关配置



\### 9. client\_base\_url（GPT-SoVITS 服务地址）

\*\*是什么\*\*：你本地 GPT-SoVITS 后端服务的网址，默认 `http://127.0.0.1:9880`。



\*\*有什么用\*\*：插件用这个地址调用它来合成语音。



\*\*出错检查\*\*：先确认 GPT-SoVITS 服务是否启动（可以浏览器访问这个地址看看有没有页面）。如果没启动，需要手动启动或开启“自动启动”。



\### 10. device（TTS 合成设备）

\*\*是什么\*\*：选 `cuda`（显卡）还是 `cpu`（处理器）。



\*\*有什么用\*\*：显卡合成快，CPU 慢。



\*\*出错检查\*\*：如果你用的是显卡但选成了 CPU，会很慢。检查你的电脑有没有 NVIDIA 显卡，有就用 `cuda`。



\### 11. auto\_start\_tts（一键启动本地 TTS 服务）

\*\*是什么\*\*：开启后，插件会自动检测并启动 GPT-SoVITS 后端服务。



\*\*有什么用\*\*：省去手动启动的麻烦。



\*\*出错检查\*\*：如果开启后没启动成功，检查 `tts\_start\_script` 路径是否正确，以及你的电脑上是否安装了 GPT-SoVITS。



\### 12. tts\_start\_script（TTS 后端启动命令）

\*\*是什么\*\*：填 GPT-SoVITS 的 `api\_v2.py` 所在文件夹的路径，比如 `E:/GPT-SoVITS`。



\*\*有什么用\*\*：插件根据这个路径去启动服务。



\*\*出错检查\*\*：路径不能写错，不能有中文字符。如果填的是文件夹，插件会自动补上 `api\_v2.py`。如果路径错了，启动会失败。



\### 13. model\_dir（模型文件夹所在路径）

\*\*是什么\*\*：存放 `.ckpt` 和 `.pth` 模型的文件夹路径。



\*\*有什么用\*\*：插件启动服务时需要加载这些模型文件。



\*\*出错检查\*\*：路径必须正确，里面必须有模型文件。如果找不到模型，插件会报错。



\### 14. ref\_audio\_root（参考音频根目录）

\*\*是什么\*\*：你所有情绪音频文件夹的父目录，比如 `D:/voice\_packs/yuqi`。



\*\*有什么用\*\*：插件从这里自动扫描每个情绪文件夹（比如 `happy`、`sad`），读取参考音频。



\*\*出错检查\*\*：路径必须正确，且不能放在系统隐藏目录（如 C 盘的 AppData）。如果扫描不到情绪，检查这个目录下是否有对应的文件夹和 `ref.mp3` 等文件。



\### 15. timeout\_seconds（TTS 超时时间）

\*\*是什么\*\*：合成语音等待的最长时间（秒）。



\*\*有什么用\*\*：如果合成太慢，超过时间就放弃。



\*\*出错检查\*\*：如果合成很慢但没超时，调大这个值；如果总是超时，可能是服务卡了。



\### 16. prompt\_text（默认参考音频文本）

\*\*是什么\*\*：默认参考音频里读的文本（比如日语）。



\*\*有什么用\*\*：当情绪文件夹里没有 `asr.txt` 时，用这个作为参考。



\*\*出错检查\*\*：如果听起来声音怪怪的，检查这个文本是否和参考音频完全一致。



\### 17. prompt\_lang（参考音频文本语言）

\*\*是什么\*\*：参考音频文本的语言代码（如 `ja` 日语、`zh` 中文）。



\*\*有什么用\*\*：告诉 TTS 参考文本是什么语言。



\*\*出错检查\*\*：如果选错语言，合成结果可能不准确。



\### 18. text\_lang（文本语言）

\*\*是什么\*\*：你要合成的台词的语言代码（如 `ja`）。



\*\*有什么用\*\*：决定插件把日语文本合成语音。



\*\*出错检查\*\*：如果生成的文本和这个语言不匹配，语音可能读不出来。确保你生成的台词确实是这个语言。



\### 19\~31. 高级 TTS 参数（top\_k、top\_p、temperature、text\_split\_method、batch\_size、batch\_threshold、split\_bucket、speed\_factor、fragment\_interval、streaming\_mode、seed、parallel\_infer、repetition\_penalty、media\_type）

\*\*这些是干什么的\*\*：它们控制语音合成的一些细节，比如随机性、速度、切分方式等。一般新手不用动。



\*\*有什么用\*\*：调整它们可以微调语音效果，比如语速快慢、语气变化大小。



\*\*出错检查\*\*：如果语音听起来很怪（比如结巴、重复），可以试着调低 `top\_k`、`top\_p`、`temperature`，或者把 `text\_split\_method` 改成 `cut0` 试试。如果声音太慢，把 `speed\_factor` 调大。



\### 32. voice\_transition（语气渐变）

\*\*是什么\*\*：开启后，不同情绪的句子之间会平滑过渡（带呼吸间隙和交叉渐变）。



\*\*有什么用\*\*：让多情绪语音听起来更自然，不突兀。



\*\*出错检查\*\*：如果开启后声音变得奇怪（比如中间有停顿），检查 `breathing\_gap\_ms` 和 `crossfade\_ms` 是否太大，可以调小。



\### 33. breathing\_gap\_ms（呼吸间隙）

\*\*是什么\*\*：两句话之间停顿的时间（毫秒）。



\*\*有什么用\*\*：模拟呼吸感。



\*\*出错检查\*\*：如果觉得停顿太长，调小到 `50\~100`。



\### 34. crossfade\_ms（交叉渐变长度）

\*\*是什么\*\*：两句话重叠过渡的时长（毫秒）。



\*\*有什么用\*\*：让切换更平滑。



\*\*出错检查\*\*：如果听起来模糊或重叠，调小它。



\### 35. send\_voice\_separately（语音分开发送）

\*\*是什么\*\*：开启后，每一句话单独发送一条语音，而不是合成一个长音频。



\*\*有什么用\*\*：可以尽快听到第一句话，不用等全部合成完。



\*\*出错检查\*\*：如果发送太快导致部分平台丢消息，可以调整代码里的发送间隔（默认 0.2 秒），或者关闭此功能。



\### 36. hide\_gsv\_options（隐藏高级 GSV 参数）

\*\*是什么\*\*：开启后，隐藏上面那些高级 TTS 参数，让配置界面更简洁。



\*\*有什么用\*\*：适合新手，避免乱调。



\*\*出错检查\*\*：如果隐藏后找不到某个参数，先关闭这个开关。



\---



\## 👤 角色与记忆相关



\### 37. character\_name（角色名称）

\*\*是什么\*\*：你的角色名，比如“丛雨”。



\*\*有什么用\*\*：用于显示在对话历史中，也让模型知道自己的名字。



\*\*出错检查\*\*：如果角色名和你的人格设定不一致，模型可能混淆。确保一致。



\### 38. character\_key（角色标识符）

\*\*是什么\*\*：用来给记忆文件命名的英文或拼音，比如 `murasame`。



\*\*有什么用\*\*：插件用它来区分不同角色的记忆文件。



\*\*出错检查\*\*：不能有中文或特殊符号，只能字母、数字、下划线。否则会报错或无法保存记忆。



\### 39. max\_voice\_cache（最大语音缓存数量）

\*\*是什么\*\*：最多保留多少个临时音频文件。



\*\*有什么用\*\*：防止磁盘被占满。



\*\*出错检查\*\*：如果磁盘空间不足，调小这个值。如果自动清理失败，可能是文件被占用。



\---



\## 🎭 情绪与提示词相关



\### 40. personality\_prompt（人格提示词）

\*\*是什么\*\*：描述你角色的性格、说话方式等。



\*\*有什么用\*\*：模型根据它来模仿角色说话。



\*\*出错检查\*\*：如果角色说话不像，检查这个描述是否完整，或者模型是否理解了。



\### 41. json\_prompt（JSON提示词）

\*\*是什么\*\*：告诉模型要输出什么格式的 JSON（包含台词和情绪）。



\*\*有什么用\*\*：插件需要这个格式来提取台词和情绪标签。



\*\*出错检查\*\*：如果模型输出格式不对（比如不是 JSON），插件会解析失败。检查这个提示词是否强制要求了正确格式。



\### 42. supplement\_prompt（补充提示词）

\*\*是什么\*\*：额外的规则，比如情绪判断规则、翻译一致性要求。



\*\*有什么用\*\*：让模型更准确地根据语境选择情绪、保持翻译一致。



\*\*出错检查\*\*：如果情绪判断总是不对，检查这里的规则是否清晰；如果翻译不一致，看看是否要求了“必须相同含义”。



\### 43. display\_lang（显示文本语言）

\*\*是什么\*\*：选择发送消息时文字的语言，比如中文、日语、英语、韩语或跟随语音。



\*\*有什么用\*\*：决定你看到的是哪种语言的文字。



\*\*出错检查\*\*：如果选了 `en` 但显示的还是中文，说明模型没有输出英文（可能 JSON 里缺 `en` 字段），需要修改 `json\_prompt` 让模型输出多种语言。



\### 44. default\_voice（默认情绪）

\*\*是什么\*\*：当不用 LLM 判断情绪时，固定使用哪个情绪文件夹。



\*\*有什么用\*\*：如果不开启情绪判断，就统一用这个声音。



\*\*出错检查\*\*：确保这个文件夹存在且里面有参考音频。



\### 45. enable\_default\_emotions（是否启用插件默认的丛雨语气）

\*\*是什么\*\*：开启后，除了你手动添加的情绪，还使用插件自带的 6 个默认情绪（比如 `pingjing` 等）。



\*\*有什么用\*\*：如果你不想用默认的，就关闭，只用手动添加的。



\*\*出错检查\*\*：如果关闭后找不到声音，检查你是否手动添加了情绪映射，或者插件默认文件夹还在不在。



\### 46. emotions\_config（情绪映射配置）

\*\*是什么\*\*：手动添加额外情绪，比如文件夹名、参考音频文件名、参考文本。



\*\*有什么用\*\*：如果你有自定义情绪，可以在这里添加。



\*\*出错检查\*\*：文件夹名、文件名、文本必须完全匹配。如果扫描不到，检查路径和文件是否存在。



\---



\## 🛠️ 其他



\### 47. llm\_judge（是否使用 LLM 自动判别情绪）

\*\*是什么\*\*：开启后，由模型根据对话判断用哪种情绪；关闭则固定用 `default\_voice`。



\*\*有什么用\*\*：让语音更符合语境。



\*\*出错检查\*\*：如果关闭了，就听不到情绪变化了。如果想用，检查模型是否正常。



\### 48. auto\_start\_tts 和 tts\_start\_script 配合使用

如果启动失败，请检查路径和权限。



\---



\## 🔍 通用出错检查步骤



1\. \*\*看日志\*\*：AstrBot 控制台会打印错误信息，比如  `JSONDecodeError`、`TTS 合成失败` 等。

2\. \*\*检查服务\*\*：确认 GPT-SoVITS 和 Ollama 是否都在运行。

3\. \*\*检查路径\*\*：所有路径是否正确。

4\. \*\*检查文件\*\*：参考音频、模型文件是否都在对应位置。

5\. \*\*配置是否保存\*\*：修改配置后记得点“保存并关闭”，然后重载插件。



\---



\## ❓ 还是不会？下面是我预设的图片链接，可以自行查看：



!\[设置1](https://raw.githubusercontent.com/slpk1ng/Local\_TTS\_Voice\_Modulation/main/guide%20images/guide1.png)





!\[设置2](https://raw.githubusercontent.com/slpk1ng/Local\_TTS\_Voice\_Modulation/main/guide%20images/guide2.png)





!\[设置3](https://raw.githubusercontent.com/slpk1ng/Local\_TTS\_Voice\_Modulation/main/guide%20images/guide3.png)





!\[设置4](https://raw.githubusercontent.com/slpk1ng/Local\_TTS\_Voice\_Modulation/main/guide%20images/guide4.png)





!\[设置5](https://raw.githubusercontent.com/slpk1ng/Local\_TTS\_Voice\_Modulation/main/guide%20images/guide5.png)





!\[自定义语气](https://raw.githubusercontent.com/slpk1ng/Local\_TTS\_Voice\_Modulation/main/guide%20images/More%20Custom%20Modulation%20guide.png)

当你决定用自己配置的语气时，记得关闭默认的丛雨语气。



!\[WebUI打开位置](https://raw.githubusercontent.com/slpk1ng/Local\_TTS\_Voice\_Modulation/main/guide%20images/v1.1.7WebUI.png)

此处从左到右第三个按钮可以打开 WebUI



!\[查看历史聊天记录](https://raw.githubusercontent.com/slpk1ng/Local\_TTS\_Voice\_Modulation/main/guide%20images/Chat%20history.png)





!\[当启用一键启动TTS服务后出现“模型加载完毕，可以开始使用了！”即可开始发送消息给bot](https://raw.githubusercontent.com/slpk1ng/Local\_TTS\_Voice\_Modulation/main/guide%20images/log%20guide.png)

当启用一键启动TTS服务后出现“模型加载完毕，可以开始使用了！”即可开始发送消息给bot

