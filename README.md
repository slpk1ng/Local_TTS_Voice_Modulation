# Local_TTS_Voice_Modulation
A smart emotion TTS plugin based on AstrBot. It uses a model to automatically figure out the context's mood and matches reference audio such as 'happy' or 'shy' to create super expressive speech. It can automatically scan external folders, has highly adjustable settings, and is easy for regular users to use.
